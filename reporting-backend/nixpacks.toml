[phases.setup]
nixPkgs = ["python311", "gcc", "freetds", "python311Packages.pip"]

[phases.install]
cmds = ["python -m pip install -r requirements.txt"]

[start]
cmd = "gunicorn --bind 0.0.0.0:$PORT --workers 4 --timeout 120 src.main:app"