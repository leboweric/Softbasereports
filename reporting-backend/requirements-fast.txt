# Optimized requirements for faster Railway builds
# Only essential packages with pinned versions for caching

# Core Flask
Flask==3.1.1
gunicorn==23.0.0

# Auth & Security
Flask-JWT-Extended==4.7.1
flask-cors==6.0.0
bcrypt==4.2.1

# Database
pymssql==2.3.6
psycopg2-binary==2.9.10
Flask-SQLAlchemy==3.1.1

# Data processing (minimal)
pandas==2.3.0
numpy==1.26.4

# Excel export (lightweight)
openpyxl==3.1.5

# API requests
requests==2.32.3

# Dependencies (auto-installed but pinned for caching)
Werkzeug==3.1.3
click==8.2.1
itsdangerous==2.2.0
Jinja2==3.1.6
MarkupSafe==3.0.2
blinker==1.9.0
PyJWT==2.10.1
SQLAlchemy==2.0.41